// 该文件仅作为备份保留，避免在 Next.js 构建时触发
// “<Html> should not be imported outside of pages/_document.” 错误
// 因此这里不再从 'next/document' 导入，也不导出实际的 Document 组件。

export default function DisabledDocumentBackup() {
  return null
}
